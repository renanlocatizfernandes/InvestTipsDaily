services:
  bot:
    build:
      context: .
      dockerfile: docker/Dockerfile
    env_file:
      - .env
    volumes:
      - ./data/zvec_db:/app/data/zvec_db
      - ./data/telegram_export:/app/data/telegram_export
    restart: unless-stopped
