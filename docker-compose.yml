services:
  bot:
    build:
      context: .
      dockerfile: docker/Dockerfile
    env_file:
      - .env
    volumes:
      - ./data/chroma_db:/app/data/chroma_db
      - ./data/telegram_export:/app/data/telegram_export
    restart: unless-stopped
